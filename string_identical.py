'''
Two strings are entered by user.check whether strings are identical or not.
'''

s1=input("Enter first string:")
s2=input("Enter second string:")

if s1==s2:
    print("both are identical")

else:
    print("both are different")
